#include "qlearning.ih"

void QLearning::giveFeedback(double reward, size_t newState)
{
//  double &Q_lastAction = d_QTable.at(d_oldstate)[d_lastAction];
//
//  double maxQNewState = std::numeric_limits<double>::lowest();
//  for (size_t action = 0; action != NR_ACTIONS; ++action)
//    maxQNewState = max(d_QTable.at(newState)[action], maxQNewState);
//
//  Q_lastAction += d_alpha * (reward + d_gamma * maxQNewState - Q_lastAction);
}
